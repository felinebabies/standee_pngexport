# VQStandeeForge 要件定義

- プロダクト名: VQStandeeForge
- 文書版: 1.0
- 作成日: 2026-06-22
- 対象: MVP
- 状態: 新規実装用

## 1. 背景と目的

CLIP STUDIO PAINTのレイヤーを外部から自動操作する方式、およびPSDへ変換する方式は採用しない。PSD変換による色味の変化を避けるため、ユーザーが制作ソフトから分類別の透過PNGを手動で書き出し、本SPAで登録、合成、一括出力する。

本SPAの目的は、以下の作業をローカルのGUI上で完結させることである。

1. 「素体」「服」「表情」「まばたき」「リップシンク」などの分類ごとに透過PNGパーツを登録する。
2. 分類の重なり順と、各分類内のパーツ候補を管理する。
3. 有効な全組み合わせをプレビューし、透過PNGとして自動生成する。
4. 宴4で利用しやすい連番ファイルと対応表をまとめて出力する。

## 2. 基本方針

- デスクトップ向けのWEB SPAとする。
- MVPはサーバーを必要としないクライアント内完結型とする。
- 読み込んだ画像は外部へ送信しない。
- `.clip`およびPSDはSPAで直接扱わない。
- 入力画像は、制作ソフトから手動で出力された透過PNGに限定する。
- 合成は元PNGと同じキャンバスサイズおよび座標を維持する。
- プレビューと書き出し結果には同一の合成処理を使用する。
- ドラッグアンドドロップによる入力画像の受付を許可する。
- 実装フレームワークやライブラリの選定は、本要件定義の対象外とする。

## 3. 対象範囲

### 3.1 MVPに含める機能

- プロジェクトの新規作成、保存、再読込
- 複数PNGのドラッグ&ドロップおよびファイル選択による登録
- 分類グループの追加、名称変更、並べ替え、有効・無効切り替え、削除
- 各グループへのパーツ登録、名称変更、並べ替え、有効・無効切り替え、削除
- グループの必須選択または任意選択の設定
- グループの合成順管理
- 現在の組み合わせのリアルタイムプレビュー
- 全組み合わせ数の算出
- 使用禁止となるパーツ組み合わせの設定
- 有効な全組み合わせの透過PNG一括生成
- 宴4向け相対パスを含む対応表の生成
- PNG、対応表、プロジェクト定義を含むZIP出力
- 書き出し前検証、進捗表示、キャンセル、エラー表示

### 3.2 MVPに含めない機能

- `.clip`、PSD、その他の制作ファイルの解析または変換
- 画像の描画、変形、トリミング、色補正
- パーツ分類のAI自動判定
- アカウント、クラウド保存、共同編集
- Unityまたは宴4への直接インポート
- 動画、アニメーションファイル、Sprite Atlasの生成

## 4. 用語とデータ構造

### 4.1 プロジェクト

一人のキャラクターに対する入力画像、グループ構成、組み合わせ規則、出力設定をまとめた単位とする。

保持項目:

- プロジェクトID
- プロジェクト名
- キャラクター識別名
- キャンバス幅、高さ
- グループ一覧
- 組み合わせ除外規則
- 命名規則
- 出力先相対パス
- 作成日時、更新日時
- データ形式バージョン

### 4.2 グループ

同時に一つだけ選択されるパーツ候補の集合とする。

標準カテゴリ:

- 素体
- 服
- 表情
- まばたき
- リップシンク
- 装飾
- その他

ユーザーは任意名称のグループを追加できる。標準カテゴリは識別用の属性であり、表示名は変更可能とする。

保持項目:

- グループID
- 表示名
- 標準カテゴリまたは「その他」
- 合成順
- 必須または任意
- 有効状態

必須グループでは、生成時に必ず一つのパーツを選択する。任意グループでは、各パーツに加えて「なし」を一つの選択肢として扱う。

### 4.3 パーツ

一つのグループに所属する透過PNG一枚を指す。

保持項目:

- パーツID
- 所属グループID
- 表示名
- 元ファイル名
- 幅、高さ
- ファイル識別用ハッシュ
- グループ内の並び順
- 有効状態

### 4.4 組み合わせ

有効な各グループから一つずつ選択したパーツの集合とする。任意グループでは「なし」も選択値に含む。

組み合わせの列挙順は、グループの並び順と各グループ内のパーツ順から決定する。同じプロジェクトと設定からは、常に同じ順序、ファイル名、対応表を生成する。

### 4.5 除外規則

全組み合わせのうち、使用できない組み合わせを除外するための規則とする。MVPでは、二つのパーツを指定するペア単位の除外規則をサポートする。

例:

- 特定の素体と特定の服を同時に使用しない。
- 特定の表情と特定のリップシンクを同時に使用しない。

除外規則に該当する組み合わせは、件数表示、プレビュー巡回、一括書き出しの対象外とする。

## 5. 入力要件

- 対応形式はPNGとする。
- 透過チャンネルを持つ画像を前提とする。
- 一つのプロジェクトに登録する全PNGは、同じ幅と高さでなければならない。
- 最初に登録したPNGの幅と高さをプロジェクトのキャンバスサイズとして設定する。
- 寸法が一致しないPNGは登録を拒否し、対象ファイル名、実寸、必要寸法を表示する。
- PNGのキャンバス内座標は変更しない。ユーザーは制作ソフト側で全パーツを同じキャンバス位置に書き出す。
- 同一内容または同一ファイル名の登録を検知した場合は警告する。
- 元ファイルは変更、上書き、削除しない。

## 6. 合成要件

- グループの合成順に従い、下層から上層へアルファ合成する。
- 一つのグループから同時に合成できるパーツは一つとする。
- 無効なグループおよび無効なパーツは、プレビューと一括生成の両方から除外する。
- 任意グループの「なし」は画像を合成しない。
- 出力画像の幅、高さ、パーツ位置は入力キャンバスと一致させる。
- 出力はRGBAの透過PNGとする。
- 透明部分を任意の背景色で埋めない。
- 中間ファイルとしてPSDを生成しない。
- プレビューに表示された組み合わせと、同じ組み合わせの出力PNGは一致しなければならない。

## 7. 画面要件

### 7.1 メイン画面

デスクトップでの利用を前提に、次の領域を持つ。

- 上部: プロジェクト作成、読込、保存、PNG登録、書き出し
- 左側: グループとパーツのツリー、並べ替え、表示状態
- 中央: 透過を示す市松背景付き合成プレビュー
- 右側: 選択中のグループまたはパーツの設定、組み合わせ選択、除外規則
- 下部または書き出し画面: 組み合わせ件数、検証結果、進捗、エラー

### 7.2 プレビュー操作

- グループごとに現在表示するパーツを選択できる。
- 任意グループでは「なし」を選択できる。
- 有効な組み合わせを前後に移動できる。
- 現在の組み合わせ名と、選択中の全パーツ名を確認できる。
- ズーム、画面内フィット、等倍表示を切り替えられる。
- ズームは表示だけに適用し、出力解像度には影響させない。

### 7.3 組み合わせ管理

- 除外規則をGUIで追加、編集、削除できる。
- 除外前の理論件数、除外件数、最終出力件数を表示する。
- グループまたはパーツの設定変更時に件数を再計算する。
- 最終出力件数が1,000件を超える場合は、処理時間と出力容量に関する警告を表示し、明示確認後に書き出す。

## 8. プロジェクト保存要件

- 作業中のプロジェクトと画像データをブラウザーのIndexedDBへ自動保存する。
- ユーザー操作によって、再利用可能なプロジェクト一式をローカルファイルへ出力できる。
- プロジェクト再読込時に、グループ、パーツ、順序、除外規則、出力設定を復元する。
- データ形式にバージョンを持たせ、将来の形式変更を判定可能にする。
- ブラウザー保存領域だけに依存せず、明示的なプロジェクト書き出し手段を提供する。

## 9. 出力要件

### 9.1 出力単位

ブラウザー互換性を確保するため、MVPの標準出力は一つのZIPファイルとする。

ZIPには次を含める。

- 全組み合わせの透過PNG
- `character_patterns.csv`
- `character_patterns.json`
- 再読込可能なプロジェクト定義

File System Access APIが利用可能な環境でのフォルダー直接出力は、追加機能とし、MVPの必須条件にはしない。

### 9.2 PNGファイル名

- 既定形式は `{character}_{index:0000}.png` とする。
- 連番は1から開始する。
- キャラクター識別名、桁数、出力先相対パスを設定できる。
- ファイル名に使用できない文字は安全な文字へ置換する。
- 同名ファイルが発生する設定では書き出しを開始せず、設定変更を促す。

出力例:

```text
Character/Sayo/sayo_0001.png
Character/Sayo/sayo_0002.png
```

### 9.3 対応表

`character_patterns.csv`はUTF-8とし、最低限次の列を持つ。

- `Pattern`: 選択されたパーツ表示名をグループ順に連結した意味名
- `FileName`: 拡張子を除いた宴4向け相対パス
- `Parts`: グループ名とパーツ名の対応

`character_patterns.json`には、CSVの内容に加えて次を保持する。

- 組み合わせID
- 各グループIDとパーツID
- 出力画像の幅、高さ
- 生成順

任意グループの「なし」は、既定では`Pattern`の意味名から省略する。CSV内に改行、カンマ、引用符を含む値がある場合は、CSV仕様に従って正しくエスケープする。

## 10. 書き出し処理要件

- 書き出し前に、入力、必須グループ、寸法、ファイル名、組み合わせ件数を検証する。
- 必須グループに有効なパーツがない場合は、書き出しを禁止する。
- PNGはメモリを解放しながら逐次または制限付き並列で生成し、大量生成時のブラウザークラッシュを防ぐ。
- 進捗として完了数、総数、現在のファイル名を表示する。
- ユーザーは処理をキャンセルできる。
- キャンセルまたは一件の失敗後も、画面を再読込せず設定を修正して再実行できる。
- 一部だけ生成された不完全なZIPを正常完了として扱わない。
- 全PNGと対応表の生成完了後にZIPを確定する。

## 11. エラーと警告

次の状態は書き出しを禁止するエラーとする。

- 有効なグループがない。
- 必須グループに有効なパーツがない。
- PNGの寸法が統一されていない。
- 有効な組み合わせが0件である。
- 出力ファイル名が重複する。
- PNGの読込またはデコードに失敗している。

次の状態は警告とする。

- 透過ピクセルが存在しないPNGが登録されている。
- 内容が同一と思われるPNGが複数登録されている。
- 最終出力件数が1,000件を超える。
- 推定出力容量が大きい。
- 標準カテゴリが重複している。

エラーと警告は、対象グループまたはファイルを特定できる文面で表示する。

## 12. 非機能要件

### 12.1 対応環境

- Windows上の最新安定版ChromeおよびEdgeを必須対応とする。
- FirefoxおよびSafariは、標準ファイル選択とZIP出力で動作可能な範囲を目標とする。
- 画面は横幅1,280px以上を主対象とする。

### 12.2 性能と安定性

- 3,000 x 4,000pxのRGBA PNGを標準的な素材として扱えること。
- 大量生成中も、進捗表示とキャンセル操作が応答すること。
- 対応ブラウザーではWeb WorkerやOffscreenCanvasの利用を検討し、非対応時は機能を維持する代替処理を持つこと。
- 同じ入力と設定から、並び順を含めて再現可能な結果を出力すること。

### 12.3 セキュリティとプライバシー

- 画像処理のためのネットワーク送信を行わない。
- ファイル名とZIP内パスを正規化し、意図しないパスを生成しない。
- ファイル名や表示名をHTMLとして解釈しない。
- 読み込んだファイルをユーザー操作なしに上書きしない。

### 12.4 操作性

- 主要操作に日本語ラベルを使用する。
- キーボード操作、フォーカス表示、フォームラベルを提供する。
- 破壊的な削除操作には確認または取り消し手段を設ける。
- 未保存または未出力の変更が失われる操作では警告する。

## 13. 受入条件

1. 同一寸法の透過PNGを「素体」「服」「表情」「まばたき」「リップシンク」の各グループへ複数登録できる。
2. グループ順を変更すると、プレビューと出力の重なり順が一致して変化する。
3. 必須・任意設定を考慮した理論組み合わせ数が正しく表示される。
4. 除外規則を追加すると、対象組み合わせが件数、巡回、出力から除外される。
5. 最終組み合わせ数と同数のRGBA透過PNGがZIPに格納される。
6. 全PNGの幅と高さがプロジェクトのキャンバス設定と一致する。
7. PNGファイル名が一意かつ決定的な連番となる。
8. CSVおよびJSONの各行・各要素が、実在するPNGと正しく対応する。
9. プロジェクトを保存して再読込した後も、画像、順序、規則、出力設定が復元される。
10. 寸法違い、必須パーツ不足、ファイル名重複などの不正状態では、理由を示して書き出しを禁止する。
11. 生成処理をキャンセルした後、画面再読込なしで再実行できる。
12. 画像の登録、プレビュー、生成に際して、画像データをネットワークへ送信しない。

## 14. 実装開始時の前提

- 本ワークスペースにある既存の`.clip`およびPNGは変更しない。
- 手動書き出しされたパーツPNGは、すべて同一キャンバス寸法、同一基準位置で用意される。
- 各グループは排他選択とし、一グループ内の複数パーツ同時表示は行わない。
- 互換性調整は、まずペア単位の除外規則で対応する。
- 宴4との連携はファイル名と対応表の生成までとし、Unityプロジェクトへの書込みは行わない。
- 技術選定後も、クライアント内完結、透過保持、同一座標、決定的出力を変更してはならない。
